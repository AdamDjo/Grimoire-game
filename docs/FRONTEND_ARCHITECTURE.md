# Frontend Architecture - Multi-Universe Narrative RPG

**Overview**: Next.js 15 (App Router) structure, Tailwind components, "Static first" approach
**Architecture**: Multi-universe from Phase 1, progressive exposure (Fantasy → 3 universes)

---

## Directory Structure

```
apps/frontend/
├── src/
│   ├── app/
│   │   ├── (auth)/
│   │   │   ├── login/
│   │   │   │   └── page.tsx
│   │   │   └── signup/
│   │   │       └── page.tsx
│   │   │
│   │   ├── (main)/
│   │   │   ├── dashboard/
│   │   │   │   └── page.tsx
│   │   │   ├── universe-select/
│   │   │   │   └── page.tsx (NEW - Universe selection)
│   │   │   ├── character-create/
│   │   │   │   └── page.tsx
│   │   │   ├── leaderboard/
│   │   │   │   └── page.tsx
│   │   │   └── settings/
│   │   │       └── page.tsx
│   │   │
│   │   ├── (game)/
│   │   │   └── session/
│   │   │       ├── [sessionId]/
│   │   │       │   └── page.tsx (MAIN GAME SCREEN)
│   │   │       └── end/
│   │   │           └── page.tsx (GAME OVER SCREEN)
│   │   │
│   │   ├── layout.tsx (root layout)
│   │   └── page.tsx (landing page)
│   │   │
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Tabs.tsx
│   │   │   └── Badge.tsx
│   │   │
│   │   ├── auth/
│   │   │   ├── LoginForm.tsx
│   │   │   └── SignupForm.tsx
│   │   │
│   │   ├── universe/ (NEW - Universe selection)
│   │   │   ├── UniverseSelector.tsx
│   │   │   ├── UniverseCard.tsx
│   │   │   └── UniversePreview.tsx
│   │   │
│   │   ├── character/
│   │   │   ├── CharacterCreator.tsx
│   │   │   ├── ClassSelector.tsx (now universe-aware)
│   │   │   ├── StatDistributor.tsx (universal stats)
│   │   │   └── StatPreview.tsx
│   │   │
│   │   ├── game/
│   │   │   ├── SceneDisplay.tsx
│   │   │   ├── ChoiceButton.tsx
│   │   │   ├── ChoicesList.tsx
│   │   │   ├── EventLog.tsx
│   │   │   └── GameOverScreen.tsx
│   │   │
│   │   ├── sidebar/
│   │   │   ├── StatsSidebar.tsx (universal stats)
│   │   │   ├── InventorySidebar.tsx
│   │   │   ├── SkillsSidebar.tsx
│   │   │   └── ReputationBar.tsx
│   │   │
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Navigation.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Sidebar.tsx
│   │   │
│   │   └── leaderboard/
│   │       ├── LeaderboardTable.tsx
│   │       └── LeaderboardTabs.tsx
│   │
│   ├── hooks/
│   │   ├── useGameState.ts
│   │   ├── useCharacter.ts
│   │   ├── useSession.ts
│   │   ├── useUniverse.ts (NEW - Universe selection)
│   │   └── useAuth.ts
│   │
│   ├── lib/
│   │   ├── utils.ts
│   │   ├── api-client.ts
│   │   └── game-constants.ts
│   │
│   ├── types/
│   │   └── index.ts (import from @grimoire/shared)
│   │
│   └── styles/
│       └── globals.css
```

---

## Detailed Pages

### 1. Landing Page (`/page.tsx`)

**Purpose**: First page before login

**Layout**:

```
┌─────────────────────────────────────────┐
│  Narrative RPG Logo       [Login] [Sign] │
├─────────────────────────────────────────┤
│                                         │
│      HEADER HERO SECTION                │
│  "Infinite universes generated by AI"   │
│  "Every game is a unique story"         │
│                                         │
│      [GET STARTED]                      │
│                                         │
├─────────────────────────────────────────┤
│  FEATURE CARDS:                         │
│  - Progression (levels, loot)           │
│  - Permanent consequences               │
│  - Multiple endings                     │
│                                         │
├─────────────────────────────────────────┤
│  Footer                                 │
└─────────────────────────────────────────┘
```

**Components Used**:

- `Header.tsx` (branding, navigation)
- `Card.tsx` (feature cards)
- `Button.tsx` (CTA buttons)

---

### 2. Login Page (`/(auth)/login/page.tsx`)

**Purpose**: User authentication

**Layout**:

```
┌─────────────────────────────────────────┐
│  Narrative RPG                          │
├─────────────────────────────────────────┤
│                                         │
│       LOGIN                             │
│                                         │
│  Email:      [________________]         │
│  Password:   [________________]         │
│                                         │
│           [Log in]                      │
│                                         │
│  No account? [Sign up]                  │
│                                         │
└─────────────────────────────────────────┘
```

**Components Used**:

- `LoginForm.tsx` (form logic)
- `Input.tsx` (form inputs)
- `Button.tsx` (submit)

**Form Validation**:

- Email required, valid email
- Password required, min 6 chars
- Show error messages

---

### 3. Signup Page (`/(auth)/signup/page.tsx`)

**Purpose**: New user registration

**Layout**: Similar to Login, but with password confirmation

**Components Used**:

- `SignupForm.tsx`
- `Input.tsx`
- `Button.tsx`

---

### 4. Dashboard (`/(main)/dashboard/page.tsx`)

**Purpose**: Main menu after login

**Layout**:

```
┌─────────────────────────────────────────┐
│ Profile: John Doe   [Settings] [Logout] │
├─────────────────────────────────────────┤
│                                         │
│      DASHBOARD                          │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │ ▶ NEW GAME                       │  │
│  │   (Create new character)         │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │ MY ONGOING GAMES                 │  │
│  │ - Thrall (Warrior) - 2h45       │  │
│  │   [Resume]                       │  │
│  │ - Elara (Mage) - 45min          │  │
│  │   [Resume]                       │  │
│  │ - Kael (Rogue) - 1h23           │  │
│  │   [Resume]                       │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │ GLOBAL STATISTICS                │  │
│  │ - Games played: 12               │  │
│  │ - Total hours: 47h12min         │  │
│  │ - Best time: 52min               │  │
│  │ - Achievements: 5/20             │  │
│  │                                  │  │
│  │ [View leaderboards]              │  │
│  └──────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

**Components Used**:

- `Header.tsx` (user info)
- `Card.tsx` (sections)
- `Button.tsx` (actions)

**Data Structure** (Mock for MVP):

```typescript
interface UserSession {
  characterName: string;
  class: string;
  level: number;
  playtimeSeconds: number;
  lastPlayedAt: Date;
}

interface UserStats {
  totalSessionsPlayed: number;
  totalHoursPlayed: number;
  bestCompletionTime: number;
  achievementsUnlocked: number;
}
```

---

### 5. Universe Selection (`/(main)/universe-select/page.tsx`) 🆕

**Purpose**: Choose the universe before character creation

**Layout**:

```
┌──────────────────────────────────────────┐
│ ◄ Back              Choose Your Universe │
├──────────────────────────────────────────┤
│                                          │
│  SELECT YOUR UNIVERSE:                   │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  🏰 VALORAIN (Fantasy)             │ │
│  │  Medieval fantasy, magic            │ │
│  │  5 classes available                │ │
│  │                                    │ │
│  │  [PLAY]                            │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  ☢️ WASTELAND (Apocalypse)         │ │
│  │  Post-apocalyptic survival         │ │
│  │  4 classes available                │ │
│  │                                    │ │
│  │  [COMING SOON] 🔒 (Phase 2B)      │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  🚀 NOVA GALAXIA (Sci-Fi)         │ │
│  │  Space exploration                  │ │
│  │  5 classes available                │ │
│  │                                    │ │
│  │  [COMING SOON] 🔒 (Phase 2B)      │ │
│  └────────────────────────────────────┘ │
│                                          │
└──────────────────────────────────────────┘
```

**Components Used**:

- `UniverseSelector.tsx` (main component)
- `UniverseCard.tsx` (individual universe card)
- `UniversePreview.tsx` (preview modal with lore)
- `Button.tsx`
- `Card.tsx`

**State Management**:

```typescript
interface UniverseState {
  selectedUniverse: UniverseType | null;
  availableUniverses: UniverseDefinition[];
  selectUniverse: (universeId: string) => void;
}
```

**Phase Rollout**:

- **Phase 1 (MVP)**: Fantasy clickable, others disabled
- **Phase 2B**: All 3 universes clickable
- **Phase 3**: + Custom universe creator

---

### 6. Character Creator (`/(main)/character-create/page.tsx`)

**Purpose**: Create a character for the selected universe

**Layout** (Fantasy example):

```
┌──────────────────────────────────────────┐
│ ◄ Back              Character Creation   │
│ Universe: 🏰 Valorain (Fantasy)          │
├──────────────────────────────────────────┤
│                                          │
│  NAME:                                   │
│  [_____________________________]         │
│  "Thrall" (min 3, max 20 chars)        │
│                                          │
│  CLASS: (5 fantasy classes)             │
│                                          │
│  ┌─────────────┐  ┌─────────────┐     │
│  │  WARRIOR    │  │    MAGE     │     │
│  │ ♥ Str +3    │  │ ◆ Int +3    │     │
│  │ ♥ HP +15    │  │ ◆ Mana +20  │     │
│  │             │  │             │     │
│  │  Combat     │  │  Magic      │     │
│  └─────────────┘  └─────────────┘     │
│                                          │
│  ┌─────────────┐  ┌─────────────┐     │
│  │   ROGUE     │  │   HEALER    │     │
│  │ ◆ Agi +3    │  │ ♥ Int +2    │     │
│  │ ◆ Luck +2   │  │ ◆ Cha +2    │     │
│  │             │  │             │     │
│  │ Stealth     │  │ Support     │     │
│  └─────────────┘  └─────────────┘     │
│                                          │
│  ┌─────────────┐                        │
│  │   RANGER    │                        │
│  │ ◆ Agi +2    │                        │
│  │ ♥ Str +1    │                        │
│  │             │                        │
│  │ Nature      │                        │
│  └─────────────┘                        │
│                                          │
│  STATS (Universal System):              │
│  HP:           [███████░░░] 70          │
│  Mana:         [█████░░░░░] 50          │
│  Strength:     [██░░░░░░░░] 10          │
│  Agility:      [██░░░░░░░░] 10          │
│  Intelligence: [██░░░░░░░░] 10          │
│  Charisma:     [██░░░░░░░░] 10          │
│  Luck:         [██░░░░░░░░] 10          │
│                                          │
│  CLASS BONUS (WARRIOR):                 │
│  Strength: 10 → 13 (+3)                 │
│  HP: 70 → 85 (+15)                      │
│                                          │
│  [Cancel]                [Start!]       │
│                                          │
└──────────────────────────────────────────┘
```

**Components Used**:

- `CharacterCreator.tsx` (main form)
- `ClassSelector.tsx` (universe-aware, loads correct classes)
- `StatDistributor.tsx` (universal stats: HP/Mana/Str/Agi/Int/Cha/Luck)
- `StatPreview.tsx` (preview with class bonuses)
- `Button.tsx`
- `Input.tsx`

**State Management** (Zustand):

```typescript
interface CharacterState {
  name: string;
  universeType: UniverseType; // 'fantasy' | 'apocalypse' | 'scifi'
  selectedClass: ClassDefinition;
  baseStats: UniversalStats;
  addPointToStat: (stat: StatKey) => void;
  removePointFromStat: (stat: StatKey) => void;
  getFinalStats: () => UniversalStats;
  isValid: () => boolean;
}

interface UniversalStats {
  hp: number;
  maxHp: number;
  mana: number;
  maxMana: number;
  strength: number;
  agility: number;
  intelligence: number;
  charisma: number;
  luck: number;
  level: number;
  xp: number;
}
```

**Notes**:

- Classes loaded dynamically based on `universeType`
- Stats adapted to context (e.g., "Mana" → "Stamina" in apocalypse, "Shield Energy" in scifi)
- Identical UI but labels change depending on universe

---

### 7. Game Session (`/(game)/session/[sessionId]/page.tsx`)

**Purpose**: MAIN GAME SCREEN - 70% of gameplay happens here

**Layout**:

```
┌────────────────────────────────────────────────────────┐
│ [Menu] Thrall | HP 85/100 | San 70/100 | Stamina 75/100│
├────────────────────────────────────────────────────────┤
│                                      │                  │
│  SCENE DISPLAY (70% width)          │ SIDEBARS (30%)   │
│  ┌──────────────────────────────┐   │ ┌──────────────┐ │
│  │ SCENE 7: Forgotten Sanctuary │   │ │ STATS        │ │
│  │                              │   │ │              │ │
│  │ You pass through the ruined  │   │ │ HP: ████░░   │ │
│  │ doors of the sanctuary. The  │   │ │ 85/100       │ │
│  │ air is charged with ancient  │   │ │              │ │
│  │ magic. At the center glows   │   │ │ San: ███░░░  │ │
│  │ a blue sphere...             │   │ │ 70/100       │ │
│  │                              │   │ │              │ │
│  │ [EVENT LOG]                  │   │ │ Stamina: ███░│ │
│  │ - You took the golden key    │   │ │ 75/100       │ │
│  │ - HP -5 from the bridge fall │   │ │              │ │
│  │ - +100 XP                    │   │ │ Level: 5     │ │
│  │                              │   │ │ XP: ██░░░░░░ │ │
│  └──────────────────────────────┘   │ │ 425/1000     │ │
│                                      │ │              │ │
│  CHOICES (4 buttons):                │ │ STR 15 INT 11│ │
│  ┌──────────────────────────────┐   │ │ AGI 15 EMP 8 │ │
│  │ ⊳ Approach the sphere        │   │ │ WIL 10       │ │
│  │   (Intellect check: 12) ✓    │   │ │              │ │
│  │   "You attempt to inspect"   │   │ │ INVENTORY:   │ │
│  │                              │   │ │ (6/10 slots) │ │
│  │ ⊳ Go to the door             │   │ │              │ │
│  │   (No prerequisite)          │   │ │ ◀ Sword      │ │
│  │   "You walk away"            │   │ │   +2 Str     │ │
│  │                              │   │ │              │ │
│  │ ⊳ Climb the staircase        │   │ │ ◀ Golden key │ │
│  │   (Agi 10, -2 fatigued) ⚠    │   │ │   Quest item │ │
│  │   "The staircase creaks..."  │   │ │              │ │
│  │                              │   │ │ ◀ Potion x2  │ │
│  │ ⊳ Examine the statue         │   │ │   Heal 50 HP │ │
│  │   (Str 15) ✓✓✓              │   │ │              │ │
│  │   "You lift..."              │   │ │ ◀ Book       │ │
│  │                              │   │ │   Lore       │ │
│  └──────────────────────────────┘   │ └──────────────┘ │
│                                      │                  │
└────────────────────────────────────────────────────────┘
```

**Components Used**:

- `SceneDisplay.tsx` (scene title + description)
- `EventLog.tsx` (last 3-4 events)
- `ChoicesList.tsx` (all 4 choices)
- `ChoiceButton.tsx` (individual choice)
- `StatsSidebar.tsx` (left sidebar stats)
- `InventorySidebar.tsx` (inventory panel)
- `Header.tsx` (top bar with quick stats)

**Interactions**:

1. User clicks choice button
2. POST `/api/game/action` { sessionId, choiceId }
3. Scene updates with animation
4. Event log updates
5. Stats potentially change

**State Management**:

```typescript
interface GameSessionState {
  sessionId: string;
  character: Character;
  currentScene: Scene;
  eventLog: LogEntry[];
  inventory: InventoryItem[];
  stats: Stats;
  level: number;
  xp: number;

  selectChoice: (choiceId: string) => Promise<void>;
  updateFromAPI: (data: APIResponse) => void;
}
```

---

### 8. Game Over Screen (`/(game)/session/end/page.tsx`)

**Purpose**: Display game summary

**Layout**:

```
┌──────────────────────────────────────────┐
│                                          │
│          VICTORY!  🎉                    │
│                                          │
│  You have saved the kingdom!             │
│                                          │
│  ────────────────────────────────────    │
│  SUMMARY:                                │
│                                          │
│  Character: Thrall (Warrior Lvl 8)      │
│  Time: 1h 45min                         │
│  Scenes traversed: 47                    │
│  Enemies defeated: 12                    │
│  Items found: 8 (2 Legendary!)           │
│  Reputation: +25 (Order of Honor)        │
│  Achievements: Rearguard, Hero           │
│                                          │
│  ────────────────────────────────────    │
│                                          │
│  [New Game]            [Back to Menu]    │
│                                          │
│  Share your victory: [Twitter]           │
│                                          │
└──────────────────────────────────────────┘
```

**Components Used**:

- `GameOverScreen.tsx` (main)
- `Card.tsx` (summary box)
- `Button.tsx`

---

### 9. Leaderboard (`/(main)/leaderboard/page.tsx`)

**Purpose**: View global scores

**Layout**:

```
┌──────────────────────────────────────────┐
│ Leaderboards                             │
├──────────────────────────────────────────┤
│                                          │
│  Tabs: [Time] [Level] [Items] [Score]   │
│                                          │
│  COMPLETION TIME                         │
│  1. 🥇 John - 42min                     │
│  2. 🥈 Alice - 48min                    │
│  3. 🥉 Bob - 51min                      │
│  4. Sarah - 1h02                        │
│  5. Mike - 1h15                         │
│                                          │
└──────────────────────────────────────────┘
```

---

### 10. Settings Page (`/(main)/settings/page.tsx`)

**Purpose**: User settings

**Options**:

- Sound on/off
- Difficulty (Easy/Normal/Hard)
- Narrative style (Dark/Comedy/Epic)
- Scene length (Short/Normal/Long)
- Theme (Light/Dark)
- Account settings

---

## Component Library (Reusable UI)

### Button.tsx

```typescript
interface ButtonProps {
  children: React.ReactNode;
  onClick?: () => void;
  disabled?: boolean;
  variant?: "primary" | "secondary" | "danger";
  size?: "sm" | "md" | "lg";
  className?: string;
}
```

### Input.tsx

```typescript
interface InputProps {
  type?: "text" | "email" | "password";
  placeholder?: string;
  value: string;
  onChange: (value: string) => void;
  error?: string;
  disabled?: boolean;
}
```

### Card.tsx

```typescript
interface CardProps {
  children: React.ReactNode;
  title?: string;
  className?: string;
  onClick?: () => void;
}
```

### Badge.tsx (for rarity)

```typescript
interface BadgeProps {
  rarity: "common" | "uncommon" | "rare" | "epic" | "legendary";
  children: React.ReactNode;
}
// Colors: white, green, blue, purple, orange
```

---

## Styling Strategy

**Colors (Tailwind)**:

```
Primary: Indigo-600 (buttons, focus)
Secondary: Slate-700 (text, backgrounds)
Success: Green-500 (positive feedback)
Warning: Yellow-500 (caution)
Danger: Red-500 (errors, death)

Rarity Colors:
- Common: White
- Uncommon: Green
- Rare: Blue
- Epic: Purple
- Legendary: Orange
```

**Typography**:

- H1: 3xl bold (page titles)
- H2: 2xl bold (section titles)
- H3: xl bold (subsection titles)
- Body: base regular (text content)
- Small: sm (descriptions)

---

## Phase 1A Milestone (Week 5)

**By end of week 5, DONE**:

- ✅ All 10 pages created + navigable
- ✅ Universe selector (Fantasy active, 2 disabled)
- ✅ All components built
- ✅ Mock data integrated (multi-universe aware)
- ✅ Responsive design (desktop first)
- ✅ No API calls yet (all static)

**Pages ready to integrate with backend**:

1. Login/Signup (auth endpoints)
2. Dashboard (session list)
3. Universe Selection (universe metadata)
4. Character Creator (POST /characters with universeType)
5. Game Session (game loop with universe context)
6. Game Over (result display)
7. Leaderboard (static data initially)
8. Settings (user prefs)

**Multi-Universe Architecture**:

- ✅ Code supports 3 universes from Phase 1
- ✅ UI shows 3 cards (2 disabled with "COMING SOON")
- ✅ ClassSelector loads correct classes based on universeType
- ✅ Stats universal system (HP/Mana/Str/Agi/Int/Cha/Luck)
- 🎯 **Exposure**: Fantasy only, Phase 2B unlocks others
